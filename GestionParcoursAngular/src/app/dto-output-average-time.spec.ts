import { DtoOutputAverageTime } from './dto-output-average-time';

describe('DtoOutputAverageTime', () => {
  it('should create an instance', () => {
    expect(new DtoOutputAverageTime()).toBeTruthy();
  });
});
